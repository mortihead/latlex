{ Заглушка для TpString: в проекте не используется ни одна уникальная
  функция этого модуля (только FSplit, которая берётся из Dos). }
unit TpString;

{$mode tp}
{$H-}

interface

implementation

end.
